[package]
name = "nano-llm"
version = "0.1.0-dev"
edition = "2021"
description = "A lightweight, single-binary LLM routing gateway in Rust"
readme = "README.md"

[dependencies]
clap = { version = "4.5", features = ["derive"] }
